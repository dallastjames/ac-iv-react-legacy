import VaultState, { initialState } from '../../models/VaultState';

export const reducer = (state: VaultState = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
